
package com.mycompany.metamorphosis.model;

/**
 *
 * @author eu 
 */
public class Resultado {

   /* private Item item1;
    private Item item2;
    private Item resultado;

    public Resultado(
        Item item1,
        Item item2,
        Item resultado) {

        this.item1 = item1;
        this.item2 = item2;
        this.resultado = resultado;
    }

    public Item getResultado() {
        return resultado;
    }

    public boolean combina(Item a, Item b) {

        return
        (item1.getNome().equals(a.getNome()) &&
         item2.getNome().equals(b.getNome()))
        ||
        (item1.getNome().equals(b.getNome()) &&
         item2.getNome().equals(a.getNome()));
    }*/

    
}
